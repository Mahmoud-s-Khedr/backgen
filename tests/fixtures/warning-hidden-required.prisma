datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model AuditEntry {
  id String @id @default(cuid())
  /// @bcm.hidden
  internalCode String
}
